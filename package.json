{
  "name": "tvconfig_formatter-action",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev:clash": "mkdir -p output && node src/main.js test/config.json -o output/site.yml -f clash",
    "dev:omnibox": "mkdir -p output && node src/main.js test/config.json -o output/omnibox.json -f omnibox",
    "dev": "pnpm dev:clash && pnpm dev:omnibox",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.19.0",
  "dependencies": {
    "psl": "^1.15.0"
  },
  "devDependencies": {
    "jest": "^30.2.0"
  }
}